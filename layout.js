export const metadata = { title: 'Retiros de Socios' };

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body style={{ margin: 0, background: '#0d0f18' }}>{children}</body>
    </html>
  );
}
